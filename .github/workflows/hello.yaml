name: Github action

on: push

jobs:
  build:
    name: Running Hello World
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2
      - name: HelloWorld.cpp
        run: echo "Hello World"
